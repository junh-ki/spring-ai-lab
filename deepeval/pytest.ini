[pytest]
testpaths = tests
# Adding the suite root to sys.path lets tests do `from datasets...` and
# `from spring_client import SpringClient` without an installable package.
pythonpath = .
addopts = -v --tb=short
